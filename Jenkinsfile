pipeline {
    agent any
    stages {
        stage('Inicio') {
            steps {
                echo '¡Funciona! Jenkins está ejecutando el pipeline desde GitHub 🚀'
            }
        }
    }
}
